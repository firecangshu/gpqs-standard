{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Deployment Readiness Schema",
  "type": "object",
  "required": ["items", "overall_readiness"],
  "properties": {
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "name", "description", "status", "priority"],
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "name_en": { "type": "string" },
          "description": { "type": "string" },
          "status": { "type": "string", "enum": ["passed", "failed", "not_checked"] },
          "priority": { "type": "string", "enum": ["high", "medium", "low"] }
        }
      }
    },
    "overall_readiness": { "type": "number", "minimum": 0, "maximum": 100 }
  }
}
